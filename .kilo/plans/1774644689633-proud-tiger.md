# Portfolio Overhaul Plan: From 6.7/10 to Near-Perfect

## Decisions

- **Chakra UI:** Migrate v2 → v3 (not replacing with Tailwind)
- **Testing:** Full stack — Vitest + React Testing Library + Playwright E2E
- **CI/CD:** GitHub Actions + Husky pre-commit hooks

---

## Phase 0: Dependency Cleanup

**Remove dead weight before migrating.**

- [ ] **0.1** Remove unused dependencies from `package.json`:
  - `js-cookie`, `@types/js-cookie`, `class-variance-authority`, `clsx`, `@emotion/styled`
- [ ] **0.2** Delete dead code files:
  - `components/ui/provider.tsx` (unused duplicate Provider)
  - Any remnants in `utils/labs/` directory
- [ ] **0.3** Delete stale non-code files:
  - `repo_context.txt`, `repo_context2.txt`, `repo_context_2scl.txt`
  - `.aider.chat.history.md`, `.aider.input.history`
- [ ] **0.4** Update `.gitignore` to include `repo_context*.txt` and `.aider*`
- [ ] **0.5** Run `pnpm install` to verify clean dependency tree

---

## Phase 1: Next.js 16 + React 19 + TypeScript 6 Migration

### 1.1 Package Upgrades

- [ ] Update `package.json` to:
  ```
  next: 16.2.1
  react: 19.2.4
  react-dom: 19.2.4
  @types/react: 19.2.14
  @types/react-dom: 19.2.3
  @types/node: 25.5.0
  typescript: 6.0.2
  eslint: 10.1.0
  eslint-config-next: 16.2.1
  markdown-to-jsx: ^9.7.13
  react-syntax-highlighter: ^16.1.1
  lucide-react: ^1.7.0
  ```
- [ ] Run `pnpm install`

### 1.2 Async Params Migration (CRITICAL — Next.js 16 hard requirement)

**Files to update:**

- [ ] `app/blog/[slug]/page.tsx` — convert `params` to `Promise<{ slug: string }>`, add `await`
- [ ] `app/blog/[slug]/UrlType.ts` — update type definition to use Promise
- [ ] `app/projects/[slug]/page.tsx` — verify already correct, update if needed
- [ ] `app/projects/[slug]/layout.tsx` — convert params if used
- [ ] All `generateMetadata` functions that receive params — must await

### 1.3 Config Updates

- [ ] Convert `next.config.mjs` → `next.config.ts`
- [ ] Add `optimizePackageImports: ["@chakra-ui/react"]` to experimental
- [ ] Update `"lint"` script: `"next lint"` → `"eslint ."`
- [ ] Add `"typecheck": "tsc --noEmit"` script

### 1.4 Handle Breaking Changes

- [ ] `markdown-to-jsx` v7 → v9: Check for API changes in `BlogPost.tsx`
- [ ] `react-syntax-highlighter` v15 → v16: Verify import paths still work
- [ ] `lucide-react` v0.563 → v1.7: Check if icon names changed
- [ ] React 19: Remove any `forwardRef` usage (ref is now a regular prop)

### 1.5 Build Verification

- [ ] Run `pnpm build` — fix all TypeScript/build errors
- [ ] Run `pnpm dev` — test all routes work

---

## Phase 2: Chakra UI v2 → v3 Migration

**19 files, 49 color mode calls, compound component API changes.**

### 2.1 Package Changes

- [ ] Uninstall: `@chakra-ui/icons`, `@chakra-ui/next-js`
- [ ] Update: `@chakra-ui/react` to `^3.34.0`
- [ ] Keep: `@emotion/react` (still required)
- [ ] Run: `npx @chakra-ui/cli snippet add` — generates UI snippets (Provider, ColorMode, Toaster, Tooltip)

### 2.2 Theme Migration (`components/theme.ts`)

- [ ] Convert `extendTheme()` → `createSystem(defaultConfig, { theme: {...} })`
- [ ] Token values need `{ value: "..." }` wrapper
- [ ] Body styles move to `globalCss` in system config
- [ ] Fix the dark-mode-only body color issue — make it respect color mode properly

### 2.3 Provider Migration

- [ ] `components/Providers.tsx`: Replace `ChakraProvider` + `ColorModeScript` with new `Provider` from generated snippets (uses `next-themes` under the hood)
- [ ] `app/layout.tsx`: Add `suppressHydrationWarning` to `<html>` tag

### 2.4 Color Mode Migration (49 calls across 12 files)

- [ ] Create `hooks/useColorModeValue.ts` — thin wrapper using `next-themes` that matches the old API signature to minimize file changes
- [ ] Replace `useColorMode()` in `ThemeToggle.tsx` with `next-themes`'s `useTheme()`
- [ ] Update all 12 files to import from the new hook location:
  - `components/BlogPost.tsx` (11 calls)
  - `components/AboutCard.tsx` (6 calls)
  - `app/about/AboutContent.tsx` (5 calls)
  - `components/PostPreview.tsx` (5 calls)
  - `components/TableOfContents.tsx` (4 calls)
  - `app/projects/[slug]/ProjectPageClient.tsx` (4 calls)
  - `app/projects/ProjectsContent.tsx` (3 calls)
  - `app/HomeContent.tsx` (3 calls)
  - `components/BlogUI.tsx` (2 calls)
  - `app/not-found.tsx` (2 calls)
  - `components/BackButton.tsx` (2 calls)

### 2.5 Icon Migration (5 files)

Replace `@chakra-ui/icons` with `lucide-react`:

- [ ] `ArrowBackIcon` → `ArrowLeft`
- [ ] `CloseIcon` → `X`
- [ ] `ArrowUpIcon` → `ArrowUp`
- [ ] `HamburgerIcon` → `Menu`
- [ ] `SunIcon` → `Sun`
- [ ] `MoonIcon` → `Moon`

### 2.6 Link Migration (7 files)

- [ ] Replace all `import { Link } from "@chakra-ui/next-js"` with `next/link`
- [ ] Apply Chakra styling via `chakra` factory or style props as needed

### 2.7 Component API Migrations

**Modal → Dialog** (`ProjectPageClient.tsx`):

- [ ] `Modal` → `Dialog.Root`, `ModalOverlay` → `Dialog.Backdrop`, `ModalContent` → `Dialog.Content`, `ModalBody` → `Dialog.Body`
- [ ] `isOpen` → `open`, `onClose` → `onOpenChange`

**Collapse → Collapsible** (`Navbar.tsx`):

- [ ] `Collapse` → `Collapsible.Root` + `Collapsible.Content`
- [ ] `in={isOpen}` → `open={isOpen}`

**Toast** (`HomeContent.tsx`):

- [ ] Replace `useToast()` with `toaster` from Chakra v3 snippet

**Divider → Separator** (AboutContent, ProjectsContent, ProjectPageClient):

- [ ] Replace all `Divider` with `Separator`

**Wrap → Flex** (AboutContent):

- [ ] Replace `Wrap`/`WrapItem` with `Flex` + `flexWrap="wrap"` + `gap`

**Boolean prop renames** (all files):

- [ ] `isOpen` → `open`
- [ ] `isDisabled` → `disabled`

**Style prop renames** (all files):

- [ ] `spacing` → `gap` on Stack/VStack/HStack
- [ ] `colorScheme` → `colorPalette` on Buttons/Tags

**Keyframes** (`loading.tsx`):

- [ ] Replace `import { keyframes } from "@emotion/react"` with Chakra v3 `keyframes` or CSS `@keyframes`

### 2.8 Build + Visual Verification

- [ ] Run `pnpm build` — zero errors
- [ ] Run `pnpm dev` — verify every page visually
- [ ] Verify dark/light mode toggle works correctly
- [ ] Verify mobile nav hamburger menu works

---

## Phase 3: ESLint 10 Flat Config

- [ ] Delete `.eslintrc.json`
- [ ] Rewrite `eslint.config.mjs`:

  ```js
  import { defineConfig, globalIgnores } from "eslint/config";
  import nextVitals from "eslint-config-next/core-web-vitals";
  import nextTs from "eslint-config-next/typescript";

  export default defineConfig([
    ...nextVitals,
    ...nextTs,
    globalIgnores([".next/**", "out/**", "build/**", "next-env.d.ts"]),
  ]);
  ```

- [ ] Run `pnpm lint` — fix all errors
- [ ] Verify TypeScript rules are active

---

## Phase 4: Motion Library Verification

- [ ] Confirm `motion` package is installed and `framer-motion` is gone
- [ ] If any files still import from `"framer-motion"`, change to `"motion/react"`
- [ ] Current scan: 0 direct framer-motion imports. Chakra v3 no longer depends on it.
- [ ] Verify `motion` is not bloating the bundle if unused directly

---

## Phase 5: Code Quality Fixes

### 5.1 Fix Server/Client Data Duplication (Architecture)

- [ ] `app/projects/[slug]/page.tsx`: Pass project data as props to `ProjectPageClient`
- [ ] `ProjectPageClient.tsx`: Remove `useParams()` + `projectsData.find()` — accept project as prop
- [ ] Eliminates dual source of truth

### 5.2 Add Error Boundary

- [ ] Create `app/error.tsx` with:
  - User-friendly error message
  - Retry button (`reset()` function)
  - Link to home page
  - Styled with Chakra v3 components

### 5.3 Add Runtime Validation (Zod)

- [ ] Install `zod`
- [ ] Create Zod schema for `PostFrontmatter` in `utils/postTypes.ts`
- [ ] Replace unsafe `as Partial<PostFrontmatter>` casts with `.safeParse()`:
  - `utils/GetPostMetadata.ts` (or renamed `getPostMetadata.ts`)
  - `app/blog/[slug]/page.tsx`
- [ ] Handle validation failures gracefully (skip invalid posts, log warnings)

### 5.4 Extract Duplicated Utilities

- [ ] Create `utils/formatters.ts` with `formatDate()` and `estimateReadingTime()`
- [ ] Update imports in `BlogPost.tsx` and `PostPreview.tsx`

### 5.5 Fix Constants Usage

- [ ] Use `CONTENT_WIDTHS` from `utils/constants.ts` across all container components
  - OR delete the unused constants if we prefer explicit values

### 5.6 Add `generateStaticParams` for Projects

- [ ] Add to `app/projects/[slug]/page.tsx`:
  ```ts
  export function generateStaticParams() {
    return projectsData.map((p) => ({ slug: p.slug }));
  }
  ```

### 5.7 Lazy-Load SyntaxHighlighter

- [ ] Use `next/dynamic` with `ssr: false` for `react-syntax-highlighter` in `BlogPost.tsx`
- [ ] Removes ~50KB from initial bundle for posts without code blocks

### 5.8 Fix Lightbox Image Optimization

- [ ] Store `StaticImageData` in lightbox state instead of string URL
- [ ] Use `next/image` in lightbox instead of raw `<img>`

### 5.9 File Naming Consistency

- [ ] Rename `GetPostContent.ts` → `getPostContent.ts`
- [ ] Rename `GetPostMetadata.ts` → `getPostMetadata.ts`
- [ ] Update all imports across the codebase

### 5.10 Accessibility Fixes

- [ ] `AboutCard.tsx`: Replace `Box` + `onClick` with proper `<a>` or add `role="link"`, `tabIndex={0}`, `onKeyDown` handler
- [ ] `not-found.tsx`: Add `as="main"` to Container
- [ ] `Navbar.tsx`: Add `aria-expanded={isOpen}` to mobile toggle button
- [ ] `ProjectPageClient.tsx`: Use descriptive alt text in lightbox (`"{project.title} - Image {index}"`)

### 5.11 Bug Fixes

- [ ] `Project.tsx:34`: Guard `images[0]` access — early return or fallback if empty
- [ ] `HomeContent.tsx:30`: Wrap `navigator.clipboard.writeText` in try/catch
- [ ] `BackButton.tsx`: Change default href from `/labs` to `/projects`
- [ ] `aboutData.ts`: Fix typo "throughoutshared" → "throughout — shared"
- [ ] `projectsData.ts`: Use `ProjectTags` constants everywhere instead of inline objects

### 5.12 Clean Dead Code

- [ ] `BlogPost.tsx`: Remove commented-out imports (HStack, Flex TODO comments)
- [ ] Remove any remaining TODO comments referencing removed features
- [ ] Remove `speeedyUrl` comment if the triple-e is intentional (add code comment explaining why)

---

## Phase 6: Testing Infrastructure

### 6.1 Vitest Setup

- [ ] Install: `pnpm add -D vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom`
- [ ] Create `vitest.config.ts`:

  ```ts
  import { defineConfig } from "vitest/config";
  import react from "@vitejs/plugin-react";

  export default defineConfig({
    plugins: [react()],
    test: {
      environment: "jsdom",
      setupFiles: ["./tests/setup.ts"],
      include: ["**/*.test.{ts,tsx}"],
    },
    resolve: { alias: { "@": "." } },
  });
  ```

- [ ] Create `tests/setup.ts` with `@testing-library/jest-dom` import
- [ ] Add scripts: `"test": "vitest", "test:run": "vitest run", "test:coverage": "vitest run --coverage"`

### 6.2 Unit Tests — Utilities

- [ ] `utils/__tests__/getPostMetadata.test.ts` — valid/invalid/missing frontmatter
- [ ] `utils/__tests__/getPostContent.test.ts` — existing/missing files
- [ ] `utils/__tests__/buildMetadata.test.ts` — metadata generation variants
- [ ] `utils/__tests__/formatters.test.ts` — formatDate, estimateReadingTime edge cases
- [ ] `utils/__tests__/postValidation.test.ts` — Zod schema validation

### 6.3 Component Tests

- [ ] `components/__tests__/Project.test.tsx` — renders, handles empty images
- [ ] `components/__tests__/PostPreview.test.tsx` — renders post metadata
- [ ] `components/__tests__/AboutCard.test.tsx` — renders, click handling, keyboard a11y
- [ ] `components/__tests__/BlogPost.test.tsx` — markdown rendering, code blocks
- [ ] `components/__tests__/TableOfContents.test.tsx` — heading rendering
- [ ] `components/__tests__/ThemeToggle.test.tsx` — toggles theme

### 6.4 Playwright E2E Setup

- [ ] Install: `pnpm add -D @playwright/test`
- [ ] Run: `npx playwright install`
- [ ] Create `playwright.config.ts`
- [ ] Add script: `"test:e2e": "playwright test"`

### 6.5 E2E Smoke Tests

- [ ] `e2e/routes.spec.ts` — every route loads without console errors
- [ ] `e2e/navigation.spec.ts` — nav links work, back button works
- [ ] `e2e/theme.spec.ts` — theme toggle works, persists across navigation
- [ ] `e2e/projects.spec.ts` — project cards link to detail pages, lightbox opens/closes
- [ ] `e2e/blog.spec.ts` — blog list shows posts, individual posts render markdown

---

## Phase 7: CI/CD Pipeline

### 7.1 GitHub Actions

- [ ] Create `.github/workflows/ci.yml`:
  ```yaml
  name: CI
  on:
    push:
      branches: [main]
    pull_request:
      branches: [main]
  jobs:
    quality:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - uses: pnpm/action-setup@v4
        - uses: actions/setup-node@v4
          with:
            node-version: 22
            cache: pnpm
        - run: pnpm install --frozen-lockfile
        - run: pnpm lint
        - run: pnpm typecheck
        - run: pnpm test:run
        - run: pnpm build
    e2e:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - uses: pnpm/action-setup@v4
        - uses: actions/setup-node@v4
          with:
            node-version: 22
            cache: pnpm
        - run: pnpm install --frozen-lockfile
        - run: npx playwright install --with-deps
        - run: pnpm build
        - run: pnpm test:e2e
  ```

### 7.2 Pre-commit Hooks

- [ ] Install: `pnpm add -D husky lint-staged`
- [ ] Run: `npx husky init`
- [ ] Configure `lint-staged` in `package.json`:
  ```json
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{json,md,css}": ["prettier --write"]
  }
  ```
- [ ] Create `.husky/pre-commit`: `npx lint-staged`
- [ ] Install Prettier: `pnpm add -D prettier`

---

## Phase 8: Final Polish

- [ ] Remove `@types/react-syntax-highlighter` if v16 ships own types
- [ ] Remove `@types/js-cookie` (already removed the package)
- [ ] Verify `tsconfig.json` settings are optimal for TS 6
- [ ] Final `pnpm build` — zero warnings, zero errors
- [ ] Final `pnpm lint` — zero warnings, zero errors
- [ ] Final `pnpm test:run` — all passing
- [ ] Final `pnpm test:e2e` — all passing
- [ ] Clean review of all changes before committing

---

## Execution Order

```
Phase 0 (Cleanup)
    ↓
Phase 1 (Next.js 16 + React 19 + TS 6)
    ↓
Phase 2 (Chakra v3) ← heaviest phase
    ↓
Phase 3 (ESLint 10) + Phase 4 (Motion) ← can run in parallel
    ↓
Phase 5 (Code Quality) ← the "senior developer" improvements
    ↓
Phase 6 (Tests) ← biggest score impact for testing category
    ↓
Phase 7 (CI/CD) ← proves the tests actually run
    ↓
Phase 8 (Final Polish)
```

## Expected Score Impact

| Category               | Before     | After       | Key Changes                                               |
| ---------------------- | ---------- | ----------- | --------------------------------------------------------- |
| Code architecture      | 7/10       | 9/10        | Error boundary, data flow fix, validation, lazy loading   |
| TypeScript proficiency | 7/10       | 9/10        | Zod validation, consistent typing, no unsafe casts        |
| Testing discipline     | 3/10       | 9/10        | Vitest + RTL + Playwright, utility + component + E2E      |
| Git/workflow hygiene   | 5/10       | 9/10        | CI/CD + pre-commit hooks + clean commits                  |
| Professional polish    | 6/10       | 9/10        | No dead code, no typos, no unused deps, consistent naming |
| **Overall**            | **6.7/10** | **~9.0/10** | All non-UI/UX dimensions addressed                        |
